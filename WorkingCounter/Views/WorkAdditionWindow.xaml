<Page
    x:Class="WorkingCounter.Views.WorkAdditionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WorkingCounter.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WorkAdditionWindow"
    Width="600"
    Height="250"
    mc:Ignorable="d">
    <Page.Resources>
        <SolidColorBrush x:Key="darkBgBrush" Color="#333" />
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="WhiteSmoke" />
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource darkBgBrush}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>


        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            Text="Title" />

        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Height="auto"
            Margin="2,0" />

        <StackPanel Grid.Row="1" Margin="2">
            <TextBlock HorizontalAlignment="Center" Text="開始日" />
            <TextBlock HorizontalAlignment="Center" Text="締切日" />
            <TextBlock HorizontalAlignment="Center" Text="ノルマ" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="2">
            <ComboBox />
            <ComboBox />
            <ComboBox />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Grid.Column="2"
            VerticalAlignment="Top"
            Orientation="Horizontal">

            <Button
                Width="100"
                Margin="5,0"
                Content="追加する" />
            <Button Width="100" Content="破棄する" />
        </StackPanel>


    </Grid>
</Page>
